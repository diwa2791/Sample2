results = []

for each payload:

    validation = validate(...)

    senderResult = send_request(...)

    results.append({

        "namespace": namespace,

        "service": service,

        "scenario": scenario,

        "endpoint": serviceConfig["endpoint"],

        "validation": "PASS",

        "validationErrors": [],

        "httpStatus": senderResult["status"],

        "responseValidation": "PASS",

        "executionTime": senderResult["executionTime"],

        "result": "PASS",

        "payload": json.dumps(payload, indent=4),

        "response": senderResult["response"]

    })

reportFile = generate_report(results)

print(f"\nHTML Report Generated : {reportFile}")
